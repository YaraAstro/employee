<?php

if ($_SERVER["REQUEST_METHOD"] == "POST") {

    // get the registration type
    $reg_type = $_POST['regType'];

    if ($reg_type == 'candidate') {

    } else {
        // recruiter
    }
}

?>